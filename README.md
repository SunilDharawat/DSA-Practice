# Learning DSA from basic to advance level
# For Pattern Problem => 
To run Pattern problems first link in html page then run code.
# For Basic Problem => 
To run code please uncomment the code then run in node js enviroment
